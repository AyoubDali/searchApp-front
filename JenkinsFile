node {

  checkout scm
  def dockerImage

  stage('Build image') {
    dockerImage = docker.build("ayoubdali/searchapp-front")
  }

  stage('Push image') {
    docker.withRegistry('', 'docker-hub-credentials') {
      dockerImage.push()
    }
  }

}